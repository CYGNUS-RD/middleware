provider = "oidc"
oidc_issuer_url = ${IAM_AUTHORITY_URL}
client_id = ${IAM_CLIENT_ID}
client_secret = ${IAM_CLIENT_SECRET}
redirect_url = "https://s3webui.cygno.cloud.infn.it/oauth2/callback"

cookie_secret = ${AUTH_SECRET}
cookie_secure = true
email_domains = [ "*" ]

http_address = "0.0.0.0:4180"
upstreams = [ "http://rcloneGUI:5572/rclone/" ]
